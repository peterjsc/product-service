FROM golang:alpine as build

WORKDIR /go/src/product-item
COPY . .
RUN apk --update add git
RUN go get -d -v ./...
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /go/bin/product ./cmd/

FROM scratch as product
COPY --from=build /go/bin/product /product
ENTRYPOINT ["/product"]

EXPOSE 8080